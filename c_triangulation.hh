#ifndef C_TRIANGULATION_HH
#define C_TRIANGULATION_HH

#include <string>

class c_data;

class c_triangulation
{
public:
    c_triangulation(void);

    bool construct(void);
};

#endif // C_TRIANGULATION_HH
